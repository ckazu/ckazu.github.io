---
pageable: true
per_page: 10
layout: blog_layout
body_class: blog
---

- if paginate
  .panel
    - if num_pages > 1
      = "Page #{page_number} of #{num_pages}"
    - if prev_page
      = link_to ' << ', prev_page
    - if next_page
      = link_to ' >> ', next_page

- page_articles.each do |article|
  %article.container
    .col-md-2
      .date= I18n.l article.date, format: :date
    .col-md-7
      %h2= link_to article.title, article
      .tags
        - article.tags.each do |tag|
          = blog_tag_for tag
      = article.summary(200)
      = link_to I18n.t('links.readmore'), article
